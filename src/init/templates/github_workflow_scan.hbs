name: Scan

on:
  push:
    branches: {{#each (lodash 'uniq' (lodash 'map' app_environments 'gha_on_push_branches'))}}
      - "{{this}}" {{/each}}

jobs:
  scan:
    uses: ./.github/workflows/_scan.yml
