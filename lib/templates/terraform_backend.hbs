terraform { 
    backend "s3" { 
        bucket = "{{terraform.namespace}}-{{terraform.backend_bucket}}"
        encrypt = true 
        dynamodb_table = "{{terraform.backend_table}}" 
        key = "terraform.tfstate"
        region = "{{terraform.aws_region}}" 
        workspace_key_prefix = "{{config.context}}" 
    } 
}